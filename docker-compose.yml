version: '3.8'

services:
  text-summarization-bot:
    build: .
    container_name: text-summarization-bot
    ports:
      - "${PORT:-8000}:8000"
    environment:
      - OPENAI_API_TOKEN=${OPENAI_API_TOKEN}
    restart: unless-stopped
    volumes:
      - ./templates:/app/templates:ro
